package MentoringwithAhmet.Interviewquestionswithdebugging;

import java.util.Scanner;

public class ReverseTheWordsandnumbers {
    public static void main(String[] args) {
        //ask user the input a word and reverse it
        //ask user to input a number and reverse it

        Scanner scanner=new Scanner(System.in);
        System.out.println("please enter a word");
        String word=scanner.nextLine();
        //System.out.println("please enter a numnber");
      //  int  usernum=scanner.nextInt();
        //you have to know the meaning of the reverse (opposite) ahmet-->temha
        //how am i goint ot reverse a string?
        //how can i reach out to each of the characters from string?
        //where am i going to store tehm as reverse?
        //I think i should use the loop to reach out each element
       /* for (int i=0; i<word.length()-1;i>=0; i++){
            String name="";
            name+=word.charAt(i);
            System.out.print(name);
        } */
    }
}
